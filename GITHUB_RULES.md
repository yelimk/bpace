# 📜 BPACE GitHub 운영 규칙 (Simple GitHub Flow)

본 문서는 **BPACE** 프로젝트의 코드 품질 관리 및 명확한 작업 기록을 위한 심플한 깃허브(GitHub) 운영 규칙입니다.

---

## 1. 🔀 브랜치 운영 방식 (Branch Strategy)

복잡한 절차 없이 **`main` 브랜치**를 중심으로 심플하게 운영합니다.

- **`main`**: 언제나 정상 실행 가능한 최신 코드를 유지하는 메인 브랜치
- **`feat/기능명`**: 새로운 기능을 개발할 때 개별 생성하는 브랜치 (예: `feat/ppg-sensor`)
- **`fix/버그명`**: 버그를 수정할 때 개별 생성하는 브랜치 (예: `fix/audio-player`)

> 💡 **Tip**: 혼자 작업 시 간단한 문서 수정이나 소규모 변경은 `main` 브랜치에 직접 커밋해도 좋습니다.

---

## 2. 📝 커밋 메시지 규칙 (Commit Convention)

커밋할 때 어떤 작업을 했는지 한눈에 알아볼 수 있도록 머리말(Prefix)을 붙입니다.

```text
<type>: 작업 내용 요약
```

### 주요 머리말 (Prefix)

| Prefix | 설명 | 커밋 예시 |
|---|---|---|
| **`feat`** | 새로운 기능 추가 | `feat: PPG 센서 심박수 측정 기능 구현` |
| **`fix`** | 버그 수정 | `fix: 오디오 플레이어 종료 시 튕김 현상 수정` |
| **`docs`** | 문서 수정 | `docs: README 실행 방법 업데이트` |
| **`style`** | UI 레이아웃, 포맷팅, 스타일 변경 | `style: 메인 화면 버튼 컬러 수정` |
| **`refactor`** | 코드 구조 개선 (기능 변경 없음) | `refactor: API 클라이언트 서비스 분리` |
| **`chore`** | 설정 파일 변경, 패키지 설치 등 | `chore: pubspec 패키지 버전 업데이트` |

---

## 3. 🔒 커밋 및 푸시 전 체크리스트

1. **분석 에러 확인**: `cd frontend` ➡️ `flutter analyze` 실행 후 에러가 없는지 확인
2. **보안 점검**: `.env`, 개인 API 키, 개인 자격 증명 파일이 커밋에 포함되지 않았는지 확인
